# Ensure you are in the right place
cd ~/aegis-ai/backend

# Create the required folders
mkdir -p agents schemas data/mock_documents

# Create the __init__.py files so Python recognizes them as modules
touch agents/__init__.py
touch schemas/__init__.py